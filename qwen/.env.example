# Qwen API Configuration
# Get your API key from: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=your-api-key-here

# Optional: Set batch size (default: 100)
# Smaller batches = more API calls but better error recovery
# Larger batches = fewer API calls but longer processing per batch
# BATCH_SIZE=100

# Optional: Set delay between batches in milliseconds (default: 2000)
# Increase if you hit rate limits, decrease for faster processing
# BATCH_DELAY=2000
